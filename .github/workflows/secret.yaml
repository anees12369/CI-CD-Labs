name: Workflow which contains a secret

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: checkout code
      uses: actions/checkout@v2

    - name: use secret
      run: echo "The secret is ${{ secrets.MY_FIRST_SECRET }}"